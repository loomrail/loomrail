{
  "name": "@loomrail/cli",
  "version": "0.1.0-alpha.2",
  "private": true,
  "type": "module",
  "bin": {
    "loomrail": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "bundle": "vite build"
  },
  "dependencies": {
    "@loomrail/daemon": "workspace:*",
    "open": "^10.0.0"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
