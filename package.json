{
  "name": "loomrail",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24.19 <25",
    "pnpm": "11.21.0"
  },
  "scripts": {
    "build": "pnpm -r --sort --if-present build",
    "dev": "pnpm build && pnpm --filter @loomrail/cli start",
    "start": "pnpm --filter @loomrail/cli start",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm build && eslint .",
    "typecheck": "pnpm build && tsc -p tsconfig.json --noEmit && pnpm -r --sort --if-present typecheck",
    "test": "node --test scripts/release-integrity.test.mjs scripts/verify-community-files.test.mjs scripts/verify-samples.test.mjs && pnpm -r --workspace-concurrency=1 --sort --if-present test",
    "test:unit": "pnpm -r --sort --if-present test:unit",
    "test:integration": "pnpm -r --sort --if-present test:integration",
    "test:fault-injection": "node scripts/run-fault-injection.mjs",
    "test:community": "node scripts/verify-community-files.mjs",
    "test:reporting": "pnpm build && pnpm --dir packages/contracts exec vitest run test/reporting.unit.test.ts && pnpm --dir packages/domain exec vitest run test/reporting.unit.test.ts && pnpm --dir packages/persistence-sqlite exec vitest run test/local-state.integration.test.ts -t \"privacy-safe reporting facts\" && pnpm --dir apps/daemon exec vitest run test/reporting.unit.test.ts test/server.integration.test.ts -t \"reporting runtime categories|privacy-safe local Insights|bounded crash preview\" --maxWorkers=1 && pnpm --dir apps/web exec vitest run src/reportDownload.test.ts",
    "test:samples": "node scripts/verify-samples.mjs",
    "test:e2e": "playwright test",
    "test:public-readiness": "node scripts/check-public-tree.mjs && node scripts/check-toolchain.mjs",
    "verify": "pnpm format:check && pnpm test:public-readiness && pnpm lint && pnpm typecheck && pnpm test",
    "bundle": "pnpm build && pnpm --filter @loomrail/cli bundle",
    "pack:release": "pnpm bundle && node scripts/pack-release.mjs",
    "test:release": "node scripts/verify-release.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.39.0",
    "eslint-plugin-react-hooks": "^6.1.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^16.0.0",
    "prettier": "^3.6.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.40.0",
    "vitest": "^4.1.0"
  }
}
