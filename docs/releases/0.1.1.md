# Loomrail 0.1.1

**Status:** Stable for macOS Apple Silicon; published

**Date:** 2026-09-12

**Dist-tag:** `latest` (`next` remains on `0.1.0-beta.1`)

**Release Support Target:** `MACOS_ARM64` (`darwin/arm64`)

## Changes

- Admits the exact locally authenticated Codex CLI `0.154.0-alpha.6.2 / darwin / arm64` target after real read-only
  success, typed failure, adapter replay and a complete production workflow through owner Acceptance.
- Keeps allowance reporting independent: no allowance row, semver range, other architecture or platform is inferred
  from execution compatibility.
- Fixes Project verification Acceptance for Plans that combine finite checks with an optional supervised `SERVE`
  recipe. Reservation and Acceptance now bind the same finite recipe set.
- Adds a repeatable public-registry Beta-to-Stable lifecycle gate covering exact installs, setup/Doctor, stopped
  backup, schema-neutral upgrade, matching-version restore and package uninstall without deleting owner data.
- Extends keyboard-first Attention coverage to three simultaneous Projects, including a repository path with spaces
  and Unicode. Existing scheduler limits continue to isolate global, per-Project, per-provider and workspace-writer
  capacity.
- Corrects operations guidance for schema-neutral upgrades and closes superseded implementation-plan statuses.

## Safety boundary

Loomrail still uses only installed, authenticated Codex CLI or Claude Code CLI sessions. There are no provider API
keys, direct provider API routes, Mock, synthetic success, automatic provider login/update, hidden fallback or
automatic Git publication. Workflow state, permissions, budgets, verification, QA and Acceptance remain owned by the
deterministic Loomrail domain.

Stable support remains limited to macOS Apple Silicon. Windows and Linux live-provider dispatch stays unsupported and
fails closed. A worktree isolates collisions but is not an operating-system sandbox.

## Verification and publication

- Exact source: `7dcacc05509eb5fbbaa4114bed30da061a29910f`.
- Exact-source [CI run 34689374255](https://github.com/loomrail/loomrail/actions/runs/34689374255) passed all six
  macOS/Windows source, browser-smoke and clean-install jobs.
- Protected [stage run 34690933465](https://github.com/loomrail/loomrail/actions/runs/34690933465) repeated source,
  fault-recovery, 65 product E2E scenarios and exact tarball verification before trusted npm staging.
- Separate owner WebAuthn approval published stage `2fcf8af5-1c39-4d89-bf64-b813d086d8ca` to `latest`.
- Registry SHA-1: `417e01bf13b60bde18d9c51668df068926a57cd6`.
- Registry integrity:
  `sha512-CaAl2rE/KpPLqfrLaWvP31st/lB5K0cTQyBVy7MQDRr3L8F3L3Nphsw8bvsuWHzYk5sZhdA0ynM2WNMmL48+bg==`.
- Provenance is recorded at
  [Sigstore log index 2807470260](https://search.sigstore.dev/?logIndex=2807470260).
- A clean public-registry lifecycle passed exact Beta-to-Stable upgrade, stopped backup, matching-version restore,
  uninstall data preservation, setup/Doctor/start/readiness/stop and Unicode/space/symlink-containment checks.
- A clean public install verified signatures for all 189 installed packages and attestations for 24 packages.
- The published package completed a real Recurkit task through `DONE / SUCCEEDED / ACCEPTED`. Four required Project
  checks and six measured Browser QA executions passed on the accepted tree. A controlled daemon restart restored the
  same terminal task, workflow and Acceptance without replay.

The run retained honest interruptions. Exhausted Claude allowance created a typed owner request instead of a
fallback; the owner explicitly selected Codex. An inaccurate `/health/ready` criterion produced a High review finding
and a typed correction request; the recorded owner decision selected the repository's existing `/health` contract.
Missing local test services and an unavailable browser target blocked verification and QA until the owner explicitly
provisioned the repository-defined dependencies and launch target. No failed or blocked result was counted as passing.

## Install

```bash
mkdir loomrail-evaluation
cd loomrail-evaluation
npm install --ignore-scripts loomrail@latest
npx playwright install chromium
npx loomrail try
```

Sign in to the official local provider CLI first. Loomrail reuses that local subscription login and does not request
or store the provider credential.
