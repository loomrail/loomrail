# Loomrail 0.1.0-alpha.5

**Status:** release candidate
**Date:** 2026-09-02
**Intended dist-tag:** `next`

The candidate is not tagged or published. Its source commit must be the clean trusted-workflow commit recorded by the
release receipt and future registry provenance; no earlier hash in this document authorizes publication.

This release candidate carries the post-alpha.4 orchestration, independent review, deterministic QA, acceptance
export, operational diagnostics, guided setup, crash recovery, and release-integrity slices. None moves workflow or
release authority into a provider, browser, in-memory worker, or unsigned receipt.

## What changed since alpha.4

- Loomrail can run up to three independent AgentRuns concurrently by default. Global, Project, provider, and
  workspace limits are validated and checked again inside the SQLite transaction that reserves each run.
- A versioned AgentProfile and immutable SquadAssignment now identify the exact role assigned to every executable
  workflow stage. ProviderSession remains a shorter provider-native session inside one durable AgentRun.
- Each new AgentRun now stores a validated effective-policy JSON plus its canonical hash: exact assignment/profile,
  provider, capability intersection, current budget revision and remaining envelope, session cap, workspace/network
  rule, model tier, claim limits and the MCP revisions present at start. The daemon consumes the snapshot for
  workspace/network access, Browser QA admission, MCP narrowing and the per-profile session backstop.
- Provider input now applies that exact built-in profile's role playbook as an intersection-only Context Pack
  refinement. Migration 30 records `ROLE_PLAYBOOK` plus profile id/revision on each new append-only recipe while
  preserving historical `WORKFLOW_TEMPLATE` recipes without fabricated provenance.
- The scheduler uses stable priority and creation order, bounded inputs, stable-checkpoint checks, and closed deferral
  reasons. It is advisory; persistence owns the final claim.
- The daemon uses a bounded pool, fills a released slot without polling, captures the exact adapter chosen for a run,
  and aborts every live provider session on shutdown.
- **Agent Fleet** shows Task, Project, role, stage, provider, running/ready/waiting status, and the exact capacity,
  checkpoint, or workspace reason behind a wait. It is reconstructed from durable state after reconnect or restart.
- Fleet supports English and Russian, equal light and dark states, native keyboard navigation, and a stacked layout at
  320 px. It cannot grant permissions, raise budgets, answer Human Requests, or make acceptance decisions.
- Migrations 20, 30 and 31 add AgentRun/SquadAssignment, role-aware Context Pack provenance and effective policy
  storage while preserving older ProviderSession/AgentRun history without fabricated backfills.
- Independent Review uses a distinct reviewer AgentRun, exact tree binding, bounded rounds, and owner-only finding
  dispositions.
- Deterministic Browser QA stores measured executions, observations, screenshots/traces, and exact tree provenance.
  Failed QA enters a bounded correction/review/retest loop; provider prose cannot waive a defect or create passing
  evidence.
- Acceptance criteria bind to current Review/QA checks. The owner can download an escaped, bounded, read-only
  acceptance summary without exposing attachment storage keys or repository paths.
- `loomrail doctor [--json]` inspects runtime, Git, storage, SQLite/migrations, and provider availability without
  starting or mutating the daemon. `loomrail data-path` separately reveals the exact local storage path.
- `loomrail setup` adds a guided zero-write Mock/Live preflight. It checks Doctor, Chromium, provider readiness and
  environment-override conflicts, then returns only closed status and owner actions; automation uses explicit mode
  and deterministic JSON.
- Live provider admission now observes a bounded exact CLI version before authentication. Only an exact verified
  matrix row can become ready; Doctor and Project Settings expose normalized closed compatibility states. The initial
  live allowlist is intentionally empty, so Auto uses Mock and explicit live choices fail before spawn.
- The named 486-test crash/fault gate kills a test-owned daemon during a durable ProviderSession and proves exactly
  one interruption report with no automatic replay after two restarts.
- Release packaging emits a closed integrity receipt with tarball and per-file digests. Clean install verifies the
  receipt, installs with dependency scripts disabled, audits the consumer graph, and then launches the package.
- Repository dependency resolution now enforces strict release age, publisher-trust no-downgrade, lockfile recheck,
  blocked exotic transitives, and reviewed lifecycle-script permissions.
- The production launcher now writes only pre-persistence-redacted, bounded operational NDJSON. It rotates 2 MiB
  segments under a 16 MiB/30-day policy; `loomrail logs export` re-redacts a complete snapshot and `logs delete`
  removes exact owned segments only while the daemon is stopped.
- Both bundled Project templates are dependency-free tested repositories with two exact Task recipes each. A public
  EN/RU catalog documents the one shipped workflow, actual standard role mapping, and owner-only acceptance boundary.
- The public repository includes structured bug and product-proposal forms, an enabled private vulnerability route,
  disabled blank external issues, and an outcome-ordered roadmap without calendar promises.
- **Insights** derives local delivery, review, QA, request, usage, and restart-recovery metrics on demand. The owner
  sees each strict aggregate or crash JSON payload in full before a one-shot download; there is no collector,
  background sender, installation identifier, persistent consent, or non-loopback reporting request.

## Safety boundary

- Provider processes start only after the durable AgentRun and workspace claim commit.
- A grant created after AgentRun start cannot widen its MCP set; revocation still closes access immediately. A
  write-enabled Codex session receives the network config key only when its run snapshot permits network.
- A provider handoff stays inside one AgentRun and does not consume another slot.
- An interrupted run is recorded as interrupted on restart and is never retried automatically.
- Role playbooks can refine context but cannot remove required workflow sections or expand permissions.
- Fleet is an authenticated bounded projection. It does not expose raw provider output or become a second queue.
- Operational logs are diagnostics rather than Events or acceptance evidence. Raw provider stdout/stderr remains
  unrecorded, and no log management route is exposed over HTTP.
- A successful `--version`, upstream release, setup run, or semver range cannot promote provider compatibility.
  Promotion requires reviewed real-stream recordings and matching macOS/Windows evidence for one exact version.
- Reporting receives only numeric counts and closed runtime categories from one SQLite statement. Unknown fields,
  free text, identifiers, paths, timestamps, logs, artifacts, messages, and stack traces fail the public schemas.

## Known limitations

- This remains pre-alpha software. Public migration and upgrade compatibility has not completed a stable release
  cycle.
- Scheduling limits currently use validated daemon configuration; a user-facing concurrency editor is deferred.
- The first squad assignment follows the fixed Standard workflow. User-imported roles, provider-native subagents, and
  a dependency graph editor are not included. Stable scope creates one immutable revision at PipelineRun start; it
  does not expose post-start squad-composition editing.
- Cost controls use the budget data providers can report; this release does not claim complete hard spend governance.
- AgentProfile model tier is captured in the immutable policy snapshot, but concrete tier-to-model mapping remains a
  future validated provider-config surface and is not claimed as enforced here.
- Desktop packaging, remote access, cloud sync, team accounts, and automatic Git publishing are not included.
- Registry provenance is required for a future stable release but does not exist for this unpublished candidate.
  Private dogfood and the complete stable-release exit gate remain open.
- No Codex or Claude Code version has a verified live matrix row in this candidate. Mock is the only admitted route
  until a separately owner-authorized, quota-bearing promotion slice is complete.

## Verification

- Contracts, domain, scheduler, persistence, daemon, web, and CLI test suites pass locally.
- Browser QA covers empty and populated Fleet states, running and waiting rows, English and Russian, light and dark,
  visible keyboard focus, 320 px without horizontal overflow, reconnect-safe refresh, and an error-free console.
- The automated Fleet E2E uses a real bounded daemon, verifies `GLOBAL_LIMIT`, and opens the selected Task with Enter.
- Production dependency audit reports no known vulnerabilities.
- The complete browser suite passes 52/52 locally.
- The named fault matrix passes 486/486 tests plus the process-boundary crash drill locally and on macOS/Windows CI.
- The `0.1.0-alpha.5` tarball contains 72 allowlisted files. Its receipt and extracted bytes match; both workspace and
  clean consumer production audits report no known vulnerability, and the launcher runs from a clean temporary
  install without dependency lifecycle scripts.
- Local log lifecycle tests pass 28/28 CLI assertions. The clean-install smoke also refuses export while the daemon
  owns its lease, then validates redacted NDJSON and exact-segment deletion after shutdown.
- Guided setup tests bring the CLI suite to 33/33. The local clean-install smoke proves non-TTY refusal, a READY Mock
  report after explicit Chromium installation, no state creation, and the existing doctor/start/log boundaries.
- Provider compatibility unit and integration tests cover exact parser states, timeout/output/error canaries,
  version-before-auth, refresh and adapter start gating locally and on macOS/Windows. This synthetic gate does not
  substitute for a real-provider matrix row.
- The bundled-sample gate validates an exact two-repository file set, rejects dependency and extra-file mutations,
  executes both source and materialized baselines, and repeats both tests from the clean release package. The local
  receipt contains 70 allowlisted files; source, fault, clean-package and 52/52 browser gates pass on macOS/Windows in
  [run 33690688589](https://github.com/loomrail/loomrail/actions/runs/33690688589).
- The public-community gate checks closed issue forms, private security routing, required safety acknowledgements,
  roadmap non-promises and cross-document links. Five policy tests plus fault, clean-package and browser gates pass
  on macOS/Windows in [run 33692732443](https://github.com/loomrail/loomrail/actions/runs/33692732443); live GitHub
  renders both forms and the private reporting route is enabled.
- Q12 local gates pass 144 contract, 197 domain, 103 persistence, 202 daemon, 70 web, and 34 CLI tests. The complete
  browser suite passes 53/53, including exact preview/download byte equality and no external request. Fault recovery,
  the 632-file public-tree check, production audit, and receipt-checked clean installation also pass. Named reporting,
  fault recovery, clean installation and 53/53 browser smoke pass on macOS/Windows in
  [run 33697965100](https://github.com/loomrail/loomrail/actions/runs/33697965100); both source jobs stop only at the
  same protected landing lint findings.
- Full `pnpm verify` is currently blocked only by three lint findings in the separately developed landing page;
  non-landing files pass ESLint. The stable release gate remains open.
- GitHub Actions [run 33668749126](https://github.com/loomrail/loomrail/actions/runs/33668749126): strengthened
  dependency policy, production audit, crash/fault gate, clean receipt/tarball installation, and browser smoke passed
  on macOS and Windows; source verify on both platforms stopped only at the same three protected landing lint
  findings.
- GitHub Actions [run 33676031870](https://github.com/loomrail/loomrail/actions/runs/33676031870): Q7 CLI tests passed
  28/28 before lint on macOS and Windows; both clean receipts, consumer audits, packaged log-lifecycle smokes and
  browser lanes passed. Both source Verify jobs again passed the fault gate and stopped only at protected landing
  lines 630, 631 and 634.
- GitHub Actions [run 33680374866](https://github.com/loomrail/loomrail/actions/runs/33680374866): Q8 CLI tests passed
  33/33 on macOS and Windows; explicit Chromium installation, both clean receipts, consumer audits, packaged guided
  setup/receipt/files/log lifecycle and 52/52 browser lanes passed. Both source Verify jobs passed the fault gate and
  stopped only at the same protected landing lines 630, 631 and 634.
- GitHub Actions [run 33686253005](https://github.com/loomrail/loomrail/actions/runs/33686253005): Q9 compatibility
  probes, production audit, crash/fault recovery, clean receipt/package installation, and 52/52 browser smoke passed
  on macOS and Windows. Both source Verify jobs stopped only at the same protected landing lines 630, 631 and 634.

## Install after publication

```bash
mkdir loomrail-evaluation
cd loomrail-evaluation
npm install --ignore-scripts loomrail@next
npx loomrail
```

For a zero-quota first run, open **Settings > AI provider**, choose **Mock**, and then initialize the demo workspace.
